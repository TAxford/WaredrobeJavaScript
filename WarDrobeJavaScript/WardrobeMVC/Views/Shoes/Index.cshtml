<body class="ShoesBody">
    @model IEnumerable<WardrobeMVC.Models.Sho>

    @{
        ViewBag.Title = "Index";
    }

    <h2>Wardrobe Shoes</h2>

    <p>
        @Html.ActionLink("Add New Shoes", "Create")
    </p>

    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-6">

                <img class="Images" src="@Html.DisplayFor(modelItem => item.Photo)" />
                <h3>@Html.DisplayFor(modelItem => item.ShoeName)</h3>
                <p>@Html.ActionLink("Edit", "Edit", new { id = item.ShoesID }) : @Html.ActionLink("Details", "Details", new { id = item.ShoesID }) : @Html.ActionLink("Delete", "Delete", new { id = item.ShoesID })</p>

            </div>
        }
    </div>
</body>


